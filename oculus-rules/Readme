從您提供的原始碼結構來看，原先的 `LB-oculus` 專案包含了 `.gitignore`、`README.md`、主程式 `bin/dbp`、多個掛載鉤子如 `hooks/dbp-autocapture` 與 `hooks/dbp-risk-gate`，以及安裝腳本 `install.sh` 與技能文件 `skill/coding-yinyang-eye/SKILL.md` 等檔案。這正是您原先設計的「本機端錄入與攔截防禦系統」。

但是，基於我們剛剛討論的「第一性原理」（放棄脆弱的本機腳本，改用遠端 CI 與聲明式規則），我們應該把這個 **Debug 庫** 與 **錄入機制** 徹底升級，讓它變成一個「AI 絕對繞不過、且人類極易維護」的防禦陣地。

以下是為您重新整理的**實戰版 Debug 庫與錄入機制 SOP**：

---

### 一、Debug 庫的真實型態：聲明式規則資料夾

不要把錯誤記在隱密的 SQLite 或 JSONL 裡。真正的 Debug 庫，應該是與專案程式碼綁定在一起、人類與 AI 都能一眼看懂的**純文字設定檔 (YAML/JSON)**。

* **建立位置**：在專案的根目錄開一個資料夾，命名為 `/.oculus-rules/`。
* **核心精神**：這裡面的每一份檔案，就是一張「曾經犯過的錯誤罰單」。一旦建檔，未來的每一次更新，系統都會自動拿這些罰單去比對新的程式碼。

### 二、錄入機制：開罰單 SOP (Zero-Code 錄入)

以前的錄入是用 `dbp` 指令在終端機打字。現在，錄入的動作變成「寫一張防禦契約」，並把它送交給遠端考官（CI）。

**情境**：AI 寫錯了折扣邏輯，把「打九折 `price * 0.9`」寫成了「加一成 `price * 1.1`」。

#### 【步驟 1】新建一張「錯誤罰單」

不要寫複雜的 Python 測試腳本，直接在 `/.oculus-rules/` 底下新增一個檔案 `bug-001-discount-logic.yml`，格式如下：

```yaml
id: bug-001
description: "AI 將折扣邏輯寫反，導致金額變大"
target_file: "src/billing.py"
# 錄入絕對不變量 (Invariant)
assertion: "calculate_discount(100) < 100" 
status: ACTIVE

```

#### 【步驟 2】觸發遠端驗證 (真正的錄入)

將這個 YAML 檔案 `git push` 到遠端 GitHub。
這就是「錄入」的最終動作。從這一秒開始，遠端的 GitHub Actions（CI 機器人）就會讀取這張罰單。如果 AI 新寫的程式碼讓 `calculate_discount(100) < 100` 這個鐵律失敗，遠端 CI 會直接亮紅燈，拒絕合併程式碼。

### 三、過渡期的「壞帳錄入機制」(KNOWN_FAIL)

如果你發現了一個系統 Bug，但現在沒空修，為了避免它變成沒人管的黑數，你需要一個嚴格的「壞帳記帳本」。

**錄入方式**：
同樣在 `/.oculus-rules/` 裡新增一張罰單，但狀態改為 `KNOWN_FAIL`，並**強制加上倒數計時器**：

```yaml
id: bug-002
description: "API 回傳的時間格式沒有統一處理時區"
status: KNOWN_FAIL
recorded_date: "2026-07-28"  # 今天的錄入日期
expire_date: "2026-08-04"    # 7 天後強制引爆

```

**遠端 CI 的執法邏輯**：

1. **TTL 逾期引爆**：每次提交程式碼時，雲端機器人會檢查 `expire_date`。如果今天已經超過 2026-08-04 卻還沒修好，原本的「警告」會直接變成「致命失敗」，整台專案停擺，逼你先修 Bug。
2. **總量硬卡死**：機器人會計算全專案裡有幾個 `KNOWN_FAIL`。只要數量超過 3 個，CI 系統直接拒絕所有新功能，直到舊債被清掉。

### 四、與原架構的對比總結

* **過去的您**：依賴 `bin/dbp` 指令來記錄，並靠 `hooks/dbp-autocapture` 在本機端攔截。缺點是 AI 只要擁有高權限，就能輕易篡改或繞過這些防線。


* **現在的機制**：錄入行為變成了「新增一份極簡的 YAML 檔案」。防線的執行權全部交給**遠端 CI 伺服器**。

透過這套設計，您的 **Debug 庫** 就是 `/.oculus-rules/` 裡的一疊檔案；您的 **錄入機制** 就是「寫 YAML + Push」。防線建置的成本降到最低，但執法強度卻達到了真正物理級別的「閉環」。
