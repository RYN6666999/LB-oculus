這是一份將原本複雜機制簡化後、能真正落地的優化 TODO 清單：

---

### 第一階段：環境隔離（解決 AI 繞過防線）

* [ ] **停用本機防禦 Hook**：清空或刪除本機的 `pre-commit` 與 `preexec` 防禦腳本，不再浪費算力在容易被 AI 繞過的本機端。
* [ ] **設定 GitHub 保護分支（Branch Protection）**：鎖定 `main` 分支，設定為「無權直接 Push」，只能透過 Pull Request 進行。
* [ ] **建立 GitHub Actions（遠端 CI）**：將所有測試流程搬到遠端 CI 執行，且將 Workflow 設定檔權限鎖死，禁止 AI 修改。

### 第二階段：防線輕量化（解決人工寫閘門瓶頸）

* [ ] **引進 Semgrep 聲明式檢查工具**：不安裝複雜的 Python 檢查腳本，改用單一 Semgrep 工具。
* [ ] **建立抓錯 YAML 模板**：抓到新錯誤時，不寫新程式，改用 10 秒鐘填寫 YAML 規則（例如：`pattern: $X == $X`），由 CI 自動載入。

### 第三階段：測試機制升級（解決 AI 自己驗自己）

* [ ] **引進「屬性測試（Property-Based Testing）」**：在核心算式寫入不變量斷言（如：轉帳前後總金額守恆），並設為自動灌入 1,000 組隨機資料測試。
* [ ] **鎖定測試腳本與介面契約（Spec）**：由人類預先寫好規格與斷言，將測試檔案設定為「唯讀」，AI 只能寫功能程式碼，不准修改測試檔案。

### 第四階段：債務硬控管（解決廢棄與壞帳累積）

* [ ] **設定 `KNOWN_FAIL` 硬性配額（上限 3 個）**：在 CI 加入計數邏輯，只要「已知壞掉」的項目超過 3 個，CI 直接拒絕任何新功能的 Merge。
* [ ] **加入 7 天自動引爆（TTL）**：要求所有 `KNOWN_FAIL` 標籤必須附帶日期（如 `KNOWN_FAIL: 2026-08-01`）。超過 7 天未修復，CI 自動將判定從「警告」轉為「致命失敗」。

### 第五階段：文件去腐化（解決靜態數字漂移）

* [ ] **清空 README 中的靜態數據**：刪除所有寫死的帳本筆數、覆蓋率等數字。
* [ ] **替換為 CI 觸發指令**：所有狀態欄位一律改成 `→ 現值：<執行 CI 指令>`，保持 Single Source of Truth。
