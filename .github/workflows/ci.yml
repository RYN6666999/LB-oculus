name: CI
on: push
jobs:
  ci:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - run: echo "hello ${{ matrix.os }}"
